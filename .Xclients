#!/bin/bash

# host-specific config
if [ -e "$HOME/.Xclients-$HOSTNAME" ]; then
    sh $HOME/.Xclients-$HOSTNAME
fi

gmpc &
devilspie &
pidgin &
fetchmail -d 60 &
unclutter -idle 1 &

emacs &
firefox &

exec gnome-session